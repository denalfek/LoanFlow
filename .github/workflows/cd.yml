name: CD

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore LoanFlow.sln

      - name: Build
        run: dotnet build LoanFlow.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test LoanFlow.sln --configuration Release --no-build --verbosity normal

  build-and-push-image:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: build-and-test
    permissions:
      contents: read
      packages: write

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    name: Deploy to Azure
    runs-on: ubuntu-latest
    needs: build-and-push-image
    environment: production
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Configure App Settings
        uses: azure/appservice-settings@v1
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          app-settings-json: |
            [
              {
                "name": "ASPNETCORE_ENVIRONMENT",
                "value": "Production"
              },
              {
                "name": "Database__ConnectionString",
                "value": "${{ secrets.PROD_DATABASE_CONNECTION_STRING }}"
              },
              {
                "name": "Database__AutoMigrate",
                "value": "${{ secrets.PROD_DATABASE_AUTO_MIGRATE }}"
              },
              {
                "name": "Database__EnableSensitiveDataLogging",
                "value": "false"
              },
              {
                "name": "Database__CommandTimeout",
                "value": "60"
              },
              {
                "name": "RabbitMQ__Host",
                "value": "${{ secrets.PROD_RABBITMQ_HOST }}"
              },
              {
                "name": "RabbitMQ__Port",
                "value": "${{ secrets.PROD_RABBITMQ_PORT }}"
              },
              {
                "name": "RabbitMQ__Username",
                "value": "${{ secrets.PROD_RABBITMQ_USERNAME }}"
              },
              {
                "name": "RabbitMQ__Password",
                "value": "${{ secrets.PROD_RABBITMQ_PASSWORD }}"
              },
              {
                "name": "RabbitMQ__VirtualHost",
                "value": "${{ secrets.PROD_RABBITMQ_VIRTUALHOST }}"
              }
            ]
